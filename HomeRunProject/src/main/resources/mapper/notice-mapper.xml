<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.kh.baseball.notice.model.dao.NoticeMapper">

	<select id="selectAllNotices" resultType="com.kh.baseball.notice.model.vo.Notice">
		SELECT
				notice_no AS noticeNo,
				title AS noticeTitle,
				content AS noticeContent,
				creation_date AS creationDate,
				attachment AS attachMent
		  FROM
		  		TB_NOTICE
		 ORDER
		 	BY
		 		NOTICE_NO DESC
	</select>
	
	<insert id="addNotice" parameterType="com.kh.baseball.notice.model.vo.Notice">
		INSERT
		  INTO
		  		TB_NOTICE
		  		(
		  		NOTICE_NO,
		  		TITLE,
		  		CONTENT,
		  		ATTACHMENT
		  		)
		 VALUES
		 		(
		 		SEQ_NOTICE_NO.NEXTVAL,
		 		#{noticeTitle},
		 		#{noticeContent},
		 		#{attachMent}
		 		)
	</insert>
	
	<update id="updateNotice" parameterType="com.kh.baseball.notice.model.vo.Notice">
		UPDATE
				TB_NOTICE
		   SET
		   		TITLE = #{Title},
		   		CONTENT = #{Content},
		   		CREATION_DATE = #{creationDate},
		   		ATTACHMENT = #{attachMent}
		  WHERE
		  		NOTICE_NO = #{noticeNo}
	</update>
	
	<delete id="deleteNotice" parameterType="_int">
		DELETE
		  FROM
		  		TB_NOTICE
		 WHERE
		 		NOTICE_NO = #{noticeNo}
	</delete>
	
	<select id="selectNoticeById" parameterType="_int" resultType="com.kh.baseball.notice.model.vo.Notice">
		SELECT
				notice_no AS noticeNo,
				title AS Title,
				content AS Content,
				creation_date AS creationDate,
				attachment AS attachMent
		  FROM
		  		TB_NOTICE
		 WHERE
		 		NOTICE_NO = #{noticeNo}
	</select>
	
	
	


</mapper>